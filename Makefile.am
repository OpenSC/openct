EXTRA_DIST = README depcomp

MAINTAINERCLEANFILES = configure install-sh ltmain.sh missing mkinstalldirs \
		       compile depcomp config.log config.status \
		       config.guess config.sub Makefile.in

SUBDIRS = etc include src
