#! /bin/sh

set -e

case "$1" in
        remove|upgrade|deconfigure)
		/etc/init.d/openct stop
		;;
        failed-upgrade)
		exit 0
		;;
	*)
		echo "prerm called with unknown argument"
		exit 1
		;;
esac

exit 0
